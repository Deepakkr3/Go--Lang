package main

import "fmt"

/*

1. methodd shuld associate with any type not mendotry to any struct only
2. there are thow type of reciver value reciver and pointer reciver
3. associate method will be also embade by default


*/

type Shape struct {
	 Ractangle
}

func (s Shape) area() int {
	return s.length * s.weadth+100000
}


type Ractangle struct {
	length int
	weadth int
}

//method with value recier
func (r Ractangle) area() int {
	return r.length * r.weadth
}

//method with pointer reciver
//you can modify ractange type instance
func (r *Ractangle) AreaPointer(factor int) int {
	r.length = r.length + factor
	return r.length * r.weadth
}

//method acosiate with any type

type MyInt int

func (i MyInt) isPosative() bool {
	return i > 0
}

//heare we are not using value only meking reciver method for type MyInt
func (MyInt) display() string {
	return "hello i am display method"
}

func main() {
	ract := Ractangle{
		length: 10,
		weadth: 20,
	}
	fmt.Println(" value ract", ract.area())
	fmt.Println("length is before ", ract.length)
	fmt.Println(" pointer ract", ract.AreaPointer(2))
	fmt.Println("length is after ", ract.length)
	x := MyInt(-10)
	fmt.Println(x.isPosative())
	fmt.Println(x.display())
	shape := Shape{
		 Ractangle{
			length: 10,
			weadth: 20,
		},
	}
	fmt.Println(shape.Ractangle.area())
}
